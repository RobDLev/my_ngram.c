// library includes
#include <stdio.h>    // Any standard library includes still use the carots
#include <stdlib.h>
#include <string.h>

// function prototypes
// do these need to be in any particular order?
int main(int argc, char** argv);
void fill_array(int* array, char* input);
void print_array(int* array, int size_array);